{
  "name": "portfolio-website",
  "version": "1.0.0",
  "description": "A simple portfolio website",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "express": "^4.18.2"
  }
}
{
  # check that push to git works
}

{

  # check that it pushes to web app
}

